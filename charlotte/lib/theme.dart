// Charlotte Theme — re-exported from the shared charlotte_ui package.
// All types (ColorSwatch9, CharlotteThemeConfig, CharlotteTheme, Glass*,
// SounderColors, sounderThemeConfig, CharlotteColors, etc.) are provided
// by the package so every existing `import '../../theme.dart'` continues
// to resolve without changes.
export 'package:charlotte_ui/theme.dart';
